# F03-001 Validation Evidence

Task: Harden v2-to-v3 migration.

Recoverable pre-change copies: `baseline/checkpoints/F03-001-prechange/` with `MANIFEST.sha256`.

## Commands

```text
python -m unittest tests.unit.test_v3_migration -v
```

Result: PASS, 7 tests. Coverage includes dry-run hash/revision manifests,
strict output and idempotence, unknown state and active-writer refusal, alias
collision refusal, interrupted-write recovery manifest, hash-safe rollback,
and preservation of non-pointer user artifacts.

```text
python scripts/validate_repository.py
```

Result: `REPOSITORY VALID`; unittest discovery ran 105 tests successfully.

## Rollback Evidence

The migrator stores the original pointer and `MANIFEST.json` before replacement.
`--rollback` restores only when the current hash matches the expected migrated
hash, so it preserves post-migration user changes.
