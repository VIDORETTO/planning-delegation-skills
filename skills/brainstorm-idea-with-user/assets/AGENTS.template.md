# AGENTS.md

## AI workflow

This project uses contract `skill-team/v3`.

Read first:

1. `docs/ai/<project-slug>/PROGRESS.md`
2. the document pointed to by `active_artifact`
3. the current-stage handoff
4. contracts referenced by the active task

`PROGRESS.md` is the only operational pointer.

Do not use this section as a status source.
Do not implement from historical chat when a validated spec exists.
Do not execute a skill different from `required_skill`.

<!-- Keep this section short. Never store current task, queue, or progress percent here. -->
