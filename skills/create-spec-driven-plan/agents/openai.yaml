interface:
  display_name: "Spec-Driven Project Planner"
  short_description: "Turn a validated brainstorm into an implementation-ready plan"
  default_prompt: "Use $create-spec-driven-plan to consume the validated brainstorm handoff, preserve stable IDs, produce and validate the planning artifacts, prepare PLAN-TO-ROUTING.md, update PROGRESS.md, and stop before routing or implementation."
